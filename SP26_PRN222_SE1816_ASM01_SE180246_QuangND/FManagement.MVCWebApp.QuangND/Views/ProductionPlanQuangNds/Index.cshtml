@model IEnumerable<FManagement.Entities.QuangND.Entities.ProductionPlanQuangNd>

@{
    ViewData["Title"] = "Production Plans";
}

<div class="container-fluid">
    <!-- Header Section -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h2 class="mb-1">
                        <i class="bi bi-calendar-check text-primary"></i> Production Plans Management
                    </h2>
                    <p class="text-muted mb-0">Manage and track your production schedules</p>
                </div>
                <div>
                    <a asp-action="Create" class="btn btn-primary btn-lg shadow-sm">
                        <i class="bi bi-plus-circle"></i> Create New Plan
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter & Search Section -->
    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form asp-action="Index" method="get">
                <div class="row g-3 align-items-end">
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-flag"></i> Plan Status
                        </label>
                        <select name="planStatus" class="form-select">
                            <option value="">All Status</option>
                            <option value="Pending" selected="@(ViewData["CurrentStatus"]?.ToString() == "Pending")">Pending</option>
                            <option value="In Progress" selected="@(ViewData["CurrentStatus"]?.ToString() == "In Progress")">In Progress</option>
                            <option value="Completed" selected="@(ViewData["CurrentStatus"]?.ToString() == "Completed")">Completed</option>
                            <option value="Cancelled" selected="@(ViewData["CurrentStatus"]?.ToString() == "Cancelled")">Cancelled</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-calendar-event"></i> From Date
                        </label>
                        <input type="date" name="fromDate" class="form-control" value="@ViewData["CurrentFromDate"]" />
                    </div>
                    <div class="col-md-2">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-calendar-event"></i> To Date
                        </label>
                        <input type="date" name="toDate" class="form-control" value="@ViewData["CurrentToDate"]" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-box"></i> Product
                        </label>
                        <select name="productId" class="form-select" asp-items="@(ViewData["Products"] as SelectList)">
                            <option value="">All Products</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <div class="d-grid gap-2 d-md-flex">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-search"></i> Search
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-x-circle"></i> Clear
                            </a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Table Section -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0" id="myTable">
                    <thead class="table-light">
                        <tr>
                            <th class="text-center">Status</th>
                            <th>Plan Date</th>
                            <th>Time Range</th>
                            <th class="text-center">Priority</th>
                            <th class="text-center">Expected Qty</th>
                            <th class="text-center">Produced Qty</th>
                            <th>Kitchen</th>
                            <th>Product</th>
                            <th>Notes</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var item in Model)
                            {
                                <tr class="@(item.IsDeleted ? "table-secondary text-muted" : "")">
                                    <td class="text-center">
                                        @{
                                            var statusClass = item.PlanStatus switch
                                            {
                                                "Pending" => "warning",
                                                "In Progress" => "info",
                                                "Completed" => "success",
                                                "Cancelled" => "danger",
                                                _ => "secondary"
                                            };
                                        }
                                        <span class="badge bg-@statusClass">@(item.PlanStatus ?? "N/A")</span>
                                    </td>
                                    <td>
                                        <i class="bi bi-calendar3 text-muted"></i>
                                        @item.PlanDate.ToString("MMM dd, yyyy")
                                    </td>
                                    <td>
                                        <small class="text-muted">
                                            <i class="bi bi-clock"></i>
                                            @if (item.StartTime.HasValue && item.EndTime.HasValue)
                                            {
                                                @item.StartTime.Value.ToString("HH:mm")
                                                <text> - </text>
                                                @item.EndTime.Value.ToString("HH:mm")
                                            }
                                            else
                                            {
                                                <text>N/A</text>
                                            }
                                        </small>
                                    </td>
                                    <td class="text-center">
                                        @{

                                            var priorityValue = item.PriorityLevel ?? 0;
                                            var priorityBadge = priorityValue switch
                                            {
                                                2 => "danger",
                                                1 => "warning",
                                                0 => "secondary",
                                                _ => "secondary"
                                            };
                                            var priorityText = priorityValue switch
                                            {
                                                2 => "High",
                                                1 => "Medium",
                                                0 => "Low",
                                                _ => "N/A"
                                            };
                                        }
                                        <span class="badge bg-@priorityBadge">@priorityText</span>
                                    </td>
                                    <td class="text-center">
                                        <strong class="text-primary">@(item.TotalExpectedQuantity ?? 0)</strong>
                                    </td>
                                    <td class="text-center">
                                        <strong class="text-success">@(item.ActualProducedQuantity ?? 0)</strong>
                                    </td>
                                    <td>
                                        <i class="bi bi-building text-muted"></i>
                                        @(item.Kitchen?.KitchenName ?? "N/A")
                                    </td>
                                    <td>
                                        <span class="badge bg-light text-dark">
                                            @(item.StoreOrderItem?.Product?.ProductName ?? "N/A")
                                        </span>
                                    </td>
                                    <td>
                                        <small class="text-muted">@(item.BatchNotes ?? "-")</small>
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Details" asp-route-id="@item.PlanId" 
                                               class="btn btn-outline-info" title="View Details">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@item.PlanId" 
                                               class="btn btn-outline-primary" title="Edit">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.PlanId" 
                                               class="btn btn-outline-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="10" class="text-center py-5">
                                    <i class="bi bi-inbox text-muted" style="font-size: 3rem;"></i>
                                    <p class="text-muted mt-2">No production plans found. Create your first plan!</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Pagination -->
    <div class="row mt-3">
        <div class="col-12">
            <nav aria-label="Table pagination">
                <ul class="pagination justify-content-center" id="pagination"></ul>
            </nav>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/table-pagination.js"></script>
    <script>
        $(function() {
            $('#myTable').createTablePagination({
                rowPerPage: 2,
            });
        });
    </script>

    <style>
        .table thead th {
            border-bottom: 2px solid #dee2e6;
            font-weight: 600;
            font-size: 0.875rem;
            padding: 0.75rem;
        }

        .table tbody td {
            padding: 0.75rem;
            vertical-align: middle;
        }

        .btn-group-sm > .btn {
            padding: 0.3rem 0.5rem;
            font-size: 0.85rem;
        }

        .badge {
            padding: 0.35em 0.6em;
            font-weight: 500;
        }
    </style>
}